---
id: 20250515-learning-linux-01
title: 리눅스 기초 / Vim 편집기
date: 2025-05-15
category: learning
subCategory: Linux
---
## 1. 리눅스(Linux) 시스템 개요

Linux는 리누스 토발즈가 UNIX를 참조하여 개발한 오픈 소스 운영체제. 윈도우와 달리 소스코드가 공개되어 있고, 이를 토대로 전 세계 개발자들이 기능을 마구마구 추가하며 Ubuntu, CentOS, Debian, Red Hat 등의 다양한 배포판을 발전시킴. 여러 사용자가 동시 접속해 각기 다른 작업을 수행할 수 있는 환경을 제공하기도 했다.

### 1.1 리눅스의 내부 구조

![](https://velog.velcdn.com/images/paramad/post/dbb19f5c-af57-49bd-b780-b205ba255dea/image.png)


\> 계층 구조를 가지고 있고, 각 층끼리 상호작용을 통해 하드웨어를 구동하고 사용자 명령을 수행함.

* **커널(Kernel)**: **하드웨어 자원을 직접 제어하고 관리**하는 가장 핵심적인 부분. 프로세스 관리, 메모리 할당, 파일 시스템 관리, 디바이스 드라이버 제어 등을 담당하며 사용자 앱이 하드웨어에 직접 접근하지 못하게 보호한다.
* **셸(Shell)**: 사용자가 입력한 명령을 해석해 커널에 전달하는 번역기, 중계자 역할. 사용자가 입력한 텍스트를 커널이 이해할 수 있도록 이진 신호로 변환한다. 종류가 여러 개인데, 가장 널리 쓰이는 건 **bash**(Bourne Again Shell). 
* **애플리케이션(Application)**: 사용자에게 특정 기능을 제공하는 프로그램 층. 웹 브라우저, 텍스트 에디터, 컴파일러, 데이터베이스가 여기에 해당한다. 애플리케이션이 하드웨어 자원을 사용해야할 때는 **시스템 호출 통로를 통해 커널에 요청을 보낸다.**

### 1.2 가상화와 접속 환경

사실 Linux의 경우 물리 서버로 다룬다기 보다 대부분 가상화 환경에서 돌리는 일이 부지기수.

* **가상화**: 하나의 컴퓨터에서 여러 OS를 동시에 돌리는 기술이다. Virtual Box, VMware, KVM 등 사용.
* **NAT**: VM이 외부 인터넷과 통신하기 위해 내부 사설 IP 주소를 외부 공인 IP 주소로 변환하는 기술. VM은 대개 호스트 컴퓨터의 IP를 빌려 인터넷에 접속한다.
* **SSH**: 원격 서버에 안전하게 접속하기 위한 보안 프로토콜. 모든 데이터가 암호화되어 전송된다. 리눅스 서버 관리에 필수.

### 1.3 셸(Shell)

#### 셸의 종류와 특징

* **Bash Shell (배시 셸)**: 리눅스의 표준 셸이자 우분투의 기본 셸. 본 셸(Bourne)과 호환되면서 다양한 기능을 포함하고 있음.
* **Dash Shell**: 가벼운 본 셸 기반의 셸로, 우분투에서 시스템 스크립트 실행용으로 널리 쓰임.
* **사용자 셸 변경**: `chsh -s [셸경로]` 명령을 사용하여 로그인 셸을 원하는 것으로 변경할 수 있다.

### 1.4 WSL
> Windows 환경에서 별도의 전통적인 가상 머신(VM)을 직접 설치하지 않고도 리눅스를 실행할 수 있도록 도와주는 도구. 리눅스의 기본 명령어, 파일 시스템, 그리고 다양한 리눅스 전용 도구들을 그대로 사용이 가능하다.

#### 장점
- 기존 무거운 가상 머신에 비해 훨씬 가볍고 빠름
- 재부팅 없이 간편 설치
- 별도의 리눅스 전용 컴퓨터 없이도 개발 가능
- Docker, Python, Git 등 리눅스 환경에 최적화된 개발 도구들을 쉽게 활용할 수 있음
- VSCode와 확장 프로그램을 통해 리눅스와 환경과 폴더 경로를 동기화해 개발할 수 있음

#### WSL 버전
WSL은 두 가지 버전으로 나뉨. 구조와 기능 면에서 차이 존재.
- WSL 1: Windows 커널 위 리눅스 API 구현. (윈도우가 리눅스인 척을 하는 개념) 파일 접근이 빠르고 윈도우와 동일한 네트워크를 사용함. **다만 Docker 사용이 제한적이다.**
- WSL 2: 가상화된 진짜 리눅스 커널을 아예 내장해 버림. 높은 시스템 호환성 제공. **온전한 리눅스 커널 환경을 제공하기 때문에 리눅스 기반 Docker를 정상적으로 사용할 수 있다.**

- - -

## 2. 터미널 프롬프트

리눅스는 CLI 환경에서 텍스트 기반의 명령어를 통해 시스템을 제어한다.

### 2.1 **프롬프트 구성**

```
사용자명@호스트명:현재디렉토리$
```

예를 들어 `ubuntu@linux-server:~$`인 경우, `ubuntu`라는 계정으로 `linux-server`라는 컴퓨터에 접속해 현재 `~` 경로에 있는 것.

* `~`: 사용자의 홈 디렉토리 약어
* 끝 기호가 `$`이면 일반 사용자, `#`이면 모든 권한을 가진 슈퍼 사용자(Root)를 의미.
* **명령어 구조**: `명령어 [옵션] [인자]` 순서로 입력하며 공백으로 구분. 옵션은 보통 `-`(한 글자)나 `--`(단어)로 시작한다.

#### Root 사용자
- 리눅스 환경에서 최고 권한(Superuser)을 가진 계정. Windows의 'Administrator(관리자)'에 대응되는 개념이다.
- 일반 사용자와 달리 시스템의 모든 파일, 설정, 사용자 계정 등에 아무런 제약 없이 접근이 가능. 진짜 말 그대로 모든 행동이 가능하다. 
- 따라서 외부 공격에 조심해야 하고 (시스템 전체가 장악당함), 중요한 시스템 파일을 삭제하거나 잘못된 명령어를 입력할 수 있다는 걸 알아야 함.
- 실무에서는 root 계정으로 직접 접속해 작업하는 경우는 거의 엇음. 최고 권한이 필요한 순간에만 `sudo` 명령어를 사용해 일시적으로 권한을 빌려 작업하는 편.

### 2.2 유용한 기본 명령어

#### 파일 및 디렉토리 조작
| 명령어 | 기능 | 주요 옵션 및 설명 |
| :--- | :--- | :--- |
| **ls** | 디렉토리 목록 확인 | `-l`(상세 정보), `-a`(숨김 파일 포함) |
| **cd** | 디렉토리 이동 | `cd ..`(상위 디렉토리), `cd ~`(홈 디렉토리) |
| **pwd** | 현재 작업 디렉토리 경로 출력 | 현재 위치를 절대 경로로 표시 |
| **mkdir** | 디렉토리 생성 | `-p`(하위 디렉토리까지 한 번에 생성) |
| **rmdir** | 빈 디렉토리 삭제 | 내용이 있는 디렉토리는 삭제 불가 |
| **rm** | 파일 또는 디렉토리 삭제 | `-r`(디렉토리 삭제), `-f`(강제 삭제) |
| **cp** | 파일/디렉토리 복사 | `-r`(디렉토리 복사 시 필수) |
| **mv** | 파일/디렉토리 이동 및 이름 변경 | `mv [원본] [대상]` 형식으로 사용 |
| **touch** | 빈 파일 생성 | 파일의 타임스탬프 업데이트 시에도 사용 |

#### 파일 내용 확인 및 편집
| 명령어 | 기능 | 주요 옵션 및 설명 |
| :--- | :--- | :--- |
| **cat** | 파일 내용 출력 | 여러 파일을 연결해서 출력 가능 |
| **more** / **less** | 화면 단위로 내용 출력 | `less`는 위아래 이동이 자유로워 권장됨 |
| **head** | 파일의 앞부분 출력 | 기본 10줄, `-n [숫자]`로 행 수 지정 |
| **tail** | 파일의 뒷부분 출력 | `-f`(실시간 로그 모니터링 시 사용) |
| **grep** | 파일 내 특정 문자열 검색 | `grep "패턴" [파일명]` |
| **find** | 파일/디렉토리 검색 | `find [경로] -name "[이름]"` |
| **vi** / **vim** | 텍스트 에디터 | 리눅스 표준 텍스트 편집 도구 |

####  권한 및 소유권 관리

| 명령어 | 기능 | 주요 옵션 및 설명 |
| :--- | :--- | :--- |
| **chmod** | 파일/디렉토리 권한 변경 | `chmod 755 [파일명]` (8진수 또는 심볼릭 모드) |
| **chown** | 소유자 및 그룹 변경 | `chown [사용자]:[그룹] [파일명]` |
| **sudo** | 관리자 권한으로 명령 실행 | 'Substitute User Do'의 약자 |
| **su** | 사용자 계정 전환 | `su -`를 통해 환경변수까지 모두 전환 가능 |

####  시스템 정보 및 프로세스 관리
| 명령어 | 기능 | 주요 옵션 및 설명 |
| :--- | :--- | :--- |
| **top** / **htop** | 시스템 리소스 및 프로세스 모니터링 | 실시간 CPU, 메모리 점유율 확인 |
| **ps** | 실행 중인 프로세스 확인 | `ps -ef`, `ps aux` 형태로 자주 사용 |
| **kill** | 프로세스 종료 | `kill -9 [PID]` (강제 종료) |
| **df** | 디스크 여유 공간 확인 | `-h`(읽기 쉬운 단위로 표시) |
| **du** | 디렉토리/파일 사용량 확인 | `-sh`(현재 디렉토리 총합 표시) |
| **free** | 메모리 사용량 확인 | `-m`(MB 단위), `-g`(GB 단위) |
| **uname** | 시스템 및 커널 정보 출력 | `-a`(모든 정보 출력) |

#### 네트워크 관련 명령어
| 명령어 | 기능 | 주요 옵션 및 설명 |
| :--- | :--- | :--- |
| **ping** | 네트워크 연결 상태 확인 | 대상 호스트와의 통신 확인 |
| **ifconfig** / **ip** | 네트워크 인터페이스 정보 확인 | 최근 리눅스에서는 `ip addr` 권장 |
| **netstat** / **ss** | 네트워크 연결 및 포트 상태 확인 | `ss -tnlp` (열려 있는 포트 확인) |
| **curl** / **wget** | 네트워크를 통한 데이터 전송/다운로드 | 웹 페이지 요청 및 파일 다운로드 |

#### 기타 유용한 명령어
*   **history**: 이전에 사용한 명령어 목록 확인
*   **clear**: 터미널 화면 정리
*   **man**: 명령어의 매뉴얼(도움말) 출력 (예: `man ls`)
*   **alias**: 별칭 지정 (자주 쓰는 명령어를 짧게 등록)
*   **tar**: 압축 및 아카이브 관리 (`-cvf` 묶기, `-xvf` 풀기)

### 2.3 입출력 리다이렉션

> 장치도 파일로 관리함!

* **표준 스트림 번호**: 0(입력), 1(출력), 2(에러)
* **출력 리다이렉션**: `>`는 기존 내용을 덮어쓰고, `>>`는 기존 내용 끝에 추가한다.
* **에러 리다이렉션**: `2>`를 사용하여 에러 메시지만 따로 파일에 기록할 수 있다. `2>&1`을 쓰면 정상 출력과 에러를 한 파일에 섞어서 저장한다.
