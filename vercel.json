{
  "version": 2,
  "builds": [
    {
      "use": "@vercel/static-build",
      "config": {
        "distDir": "public",
        "buildCommand": "mkdir public; python3 generate_posts.py; cp index.html posts.json public/; cp -r assets files public/"
      }
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
